{
  "name": "tw-music-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./src/style.css -o ./dist/style.css --watch",
    "minify": "npx tailwindcss -i ./src/style.css -o ./dist/style.min.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-plugin-tailwindcss": "^0.3.0",
    "tailwindcss": "^3.3.2"
  },
  "dependencies": {
    "tailwind-scrollbar-hide": "^1.1.7"
  }
}
